{
  "name": "coffeenode-chr",
  "version": "1.0.4",
  "description": "A UCS-2-aware library for the representation & transformation of Unicode characters & codepoints.",
  "author": {
    "name": "loveencounterflow",
    "email": "paragate@gmx.net"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/loveencounterflow/coffeenode-chr.git"
  },
  "devDependencies": {
    "guy-test": "^1.0.5"
  },
  "dependencies": {
    "cnd": "^3.0.4"
  },
  "main": "./lib/main",
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "build": "coffee --map -o lib -c src",
    "push": "git add . && git commit -m'update' && git push -u origin master",
    "pub": "npm run build && npm run push && npm version patch && npm publish",
    "test": "npm run build && node lib/tests.js"
  },
  "bugs": {
    "url": "https://github.com/loveencounterflow/coffeenode-chr/issues"
  },
  "_id": "coffeenode-chr@0.1.1",
  "license": "MIT",
  "readmeFilename": "README.md",
  "readme": "A UCS-2-aware library for the representation & transformation of Unicode characters & codepoints."
}
